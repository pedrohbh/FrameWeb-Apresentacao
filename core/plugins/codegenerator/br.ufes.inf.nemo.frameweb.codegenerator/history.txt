~ Estudo da arquitetura do Eclipse 4 RCP (basicamente não apliquei no projeto...)
~ ~ Muito útil para entender como a plataforma funciona, facilitando bastante na resolução de inúmeros problemas
	encontrados durante o desenvolvimento da ferramenta.

~ Faceta criada utilizando extension points

~ Separação do template de atributo entre template de atributo e 'getter and setter'

~ Criação dos templates referentes as anotações de ORM

~ Mudança na forma de renderização dos templates, agora é utilizado o Velocity Template Expression
~ ~ Talvez a existência de vários atributos referentes aos templates seja desnecessária...
~ ~ Ainda existem problemas para a utilização das generalizações no template
~ ~ Ainda existem problemas para a utilização das associações no template (urgente)